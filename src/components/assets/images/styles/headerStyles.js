import { makeStyles } from '@material-ui/core'

const headerStyle = makeStyles(() => {
    return {
        heading: {
            fontSize: '22px',
            fontWeight: 'bold',
            color: '#172b4d'
        }
    }
})

export default headerStyle
